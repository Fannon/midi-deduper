{
  "name": "midi-deduper",
  "version": "0.3.0",
  "description": "Small web app that helps to deduplicate accidental MIDI double triggers (e.g. by finger drum pads)",
  "main": "index.js",
  "scripts": {
    "build": "bash ./bin/updateLibs.sh",
    "deploy": "gh-pages -d ./web && echo https://fannon.github.io/midi-deduper/",
    "start": "live-server ./web"
  },
  "author": "Simon Heimler",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^5.3.8",
    "webmidi": "^3.1.14"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "live-server": "^1.2.2"
  }
}
